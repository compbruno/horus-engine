package engine;

import engine.core.Engine;

public class Main {
    public static void main(String[] args) {
        Engine engine = new Engine();
        engine.run();
    }
}
// This is the main class of the Horus Engine. It initializes and runs the engine.
// The main method creates an instance of the Engine class and calls its run method to start the engine.